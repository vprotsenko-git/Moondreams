services:
  backend:
    image: moondreams-backend:latest
    build:
      context: ../Moondreams-backend
      dockerfile: Dockerfile
    volumes:
      - ../Moondreams-backend/models:/app/models
    networks:
      - moondreams-net

  frontend:
    image: moondreams-frontend:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - moondreams-net

networks:
  moondreams-net:
    driver: bridge